We built a zero-budget Threat Intelligence Engine that turns a single IP address into a clear, actionable risk assessment in seconds.

Instead of manually checking multiple platforms, our system automatically enriches data from VirusTotal, AbuseIPDB, and OTX, unifies the signals, and delivers a structured risk verdict with analyst-friendly reporting.

It transforms scattered threat intelligence into one clean, real-time decision layer — accessible via API or CLI — built with production-grade architecture and no paid infrastructure



use ur own api keys 

$env:VIRUSTOTAL_API_KEY=""
$env:ABUSEIPDB_API_KEY=""
$env:OTX_API_KEY=""
